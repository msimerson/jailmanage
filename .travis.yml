language: bash

cache:
  directories:
#   - /home/travis/.stack

addons:
  apt:
    sources:
      - debian-sid
    packages:
      - shellcheck

before_install:
    - sudo -E apt-get -yq --no-install-suggests --no-install-recommends --allow-unauthenticated install shellcheck 

install:
    - test -f jailmanage.sh

before_script:

script:
   - bash -n *.sh
   - shellcheck -e SC2039 *.sh

after_success:
after_failure:
after_script:

